name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        # Stable bun only - canary builds produce broken installs (AGENTS.md #10)
        with:
          bun-version: 1.4.1
      - run: bun install --frozen-lockfile
      - run: bun run verify
      - run: bunx tsc -p packages/ionic-everywhere/tsconfig.json --noEmit
      - run: bun run build

  scaffold-smoke:
    # FEAT-035: run on both OSes - path/PATHEXT regressions (gradlew, where.exe
    # probes) previously shipped undetected with an ubuntu-only smoke job.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.1
      - name: Cache bun install cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: bun-${{ runner.os }}-
      - run: bun install --frozen-lockfile
      # End-to-end: build CLI, scaffold web+android+desktop app, assert output,
      # then web-build the result (FEAT-027) to catch dependency drift.
      # Needs no JDK/Android SDK - `cap add android` only copies the platform.
      - run: bun scripts/cli-test.mjs
      # FEAT-029: age a fresh scaffold, `upgrade --yes`, assert restoration.
      - run: bun scripts/cli-test-upgrade.mjs
